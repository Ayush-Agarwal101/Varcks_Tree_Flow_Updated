file: project_blueprint/backend/node/nestjs/tsconfig.json
functions:
- name: configure_compiler
  parameters:
  - name: options
    type: object
    entity: compiler
  return_type: object
  entity: compiler
  produces: []
  description: Configures the TypeScript compiler with the given options.
- name: resolve_modules
  parameters:
  - name: names
    type: array
    entity: module
  return_type: array
  entity: module
  produces:
  - module.paths
  description: Resolves the given module names to their corresponding file paths.
